# Mirsad — consolidated requirements and dependencies ledger

**Production update (22 September, 19:43 Berlin): Supabase is connected and cloud login/logout, public-market reads, durable isolated simulation and responsive UI are verified.** All seven private tables have RLS enabled and no API-role SELECT privileges. 69 isolated tests pass. Remaining external dependencies are private Revolut X credentials and optional IBKR authorization/infrastructure; live sending remains locked.

Updated 2026-09-22, Europe/Berlin. **This is the single requirements ledger.** Status terms: **implemented** means code exists; **locally verified** means local/test evidence exists; **externally blocked** means deployment, credentials, account approval, or user action is still required. None of these labels by itself means a live account is connected or real trading is verified.

| ID | Consolidated requirement | Implementation / evidence | Remaining dependency and status |
|---|---|---|---|
| R01 | Inspect access, reuse matching work, preserve unrelated resources | GitHub `Exxd00` inspected; no matching project identified, then private `Exxd00/mirsad-trading` created. Actual browser Vercel team `ixa1` has the dedicated `mirsad-trading` project. Unrelated connector inventory was not used to alter other projects. | **Locally/external access verified.** Reuse these exact resources on continuation. |
| R02 | Save recoverable source privately and deploy on Vercel | Full-source GitHub checkpoint `bf7060671d53fb8397f55a3934ce44349ff7d826`; initial deployment published from actual Vercel UI at https://mirsad-trading.vercel.app. | **Published and production auth/storage verified.** Exact deployment and evidence in HANDOFF.md. Private broker account connection remains separate. |
| R03 | Arabic RTL responsive desktop/mobile PWA | Login, market desk, accounts, activity/settings, watchlists/search, charts, responsive ticket and confirmation implemented. Manifest/icons/offline page included; service worker caches only public offline assets. Local desktop 1920×850 and mobile 390×844 checked without horizontal overflow. | **Implemented; local and published authenticated desktop/mobile views verified.** Browser offers PWA installation; install itself was not performed. |
| R04 | Real server authentication and security on every private page/API; safe secrets | Salted scrypt hash only, random hashed durable session tokens, HttpOnly/SameSite=Strict cookies with Secure in production, origin+CSRF validation, database rate limits, logout and password change/session invalidation. AES-256-GCM broker secret encryption bound to broker context; no public secret variables. | **Implemented; cloud login/logout/new login, anonymous page/API rejection, nonce CSP and cookie flags verified.** Seven production tables have RLS; API roles lack SELECT grants. Preview secrets remain separate. |
| R05 | Durable persistence and appropriate serverless operation | SQL state stores sessions, settings, encrypted credentials, intent/idempotency state, simulator ledger, and minimal audit events. Development uses durable local PGlite. Production refuses local/in-memory fallback. Single-flight UI polling and source timestamps expose stale/delayed data. | **Production Supabase storage verified.** Raw DATABASE_PASSWORD override resolves authentication; simulation persisted through reload and logout/new login. Free-plan availability limits remain. |
| R06 | Use official Revolut X API available to actual account; separate account types | Official EEA public/private adapter, Ed25519 signing and authenticated credential form implemented. Banking, retail stocks and Revolut X are not treated as one balance. Actual user's X API page showed no API keys. | **Adapter implemented; account connection externally blocked.** User must register keys and enter secrets securely after server readiness. No private API reads or verified trading scope yet. No Revolut retail stock API assumed. |
| R07 | IBKR stocks/ETF and other supported markets only with actual authorization | Official API, Germany/account, session, subscriptions, Gateway/OAuth and hosting constraints researched in BROKERS.md. Separate disconnected account placeholder present. | **Not connected; operational adapter not implemented.** No existing account/API authorization found. Requires authorized account, entitlements and suitable persistent authenticated gateway/approved OAuth. Do not claim stocks support is live. |
| R08 | Real searchable market data, charts, bid/ask/spread, source/time/status | Official public Revolut X EEA reads verified: 385 instruments, BTC-EUR ticker/book and 1000 candles. UI exposes 1/5/15/60-minute views, metadata, quote age, delayed/stale states; older responses are discarded after symbol changes. | **Implemented; public-market reads verified locally and in production.** Cloud UI shows official ticker/book/candles with source time. This does not prove private account entitlement or fill prices. |
| R09 | Actual balances, available cash, positions, orders/fills and trustworthy P&L | Revolut private read paths and separate account UI implemented. Periodic reads can reflect broker-platform operations once connected. Order states, partial filled quantity, fees, and truncated-history warnings displayed. Missing cost basis/FX/fees yield unavailable P&L, not invented values. | **Implemented, mock/simulation verified; real account externally blocked.** Account attribution, balances, full history, actual fees/P&L, and buying power unverified until authorized private reads. No merged balance or assumed buying power. |
| R10 | Manual order entry/review/explicit confirm, default live lock, disable switch | Market/base-quantity and GTC-limit forms only; server revalidates account, origin/session, key identity, scope declaration, EEA confirmation, balance/increments/minimums, quote freshness and preview expiry. Each real order requires user activation then individual confirmation. Disable switch stops new sends only. | **Implemented and locally tested; live locked.** No real order/cancellation/transfer tested. No autonomous strategy, auto-reinvestment, added leverage or capability scope. |
| R11 | Prevent duplicate financial orders; reconcile ambiguity before retry | Persistent unique intent/idempotency keys, atomic account/intent locks, one broker POST per consumed intent, stable client IDs, and durable UNKNOWN/SUBMITTING states. Explicit reconciliation queries broker identity/order; new owner session may reconcile old intent, while confirmation remains session-bound. | **Implemented and isolated-test verified.** Real broker ambiguity/egress/account outcome unverified. An absent lookup is not permission to resubmit. |
| R12 | Isolated labelled order-cycle simulation, including partial fill/reservations and faults | Server `/simulation` namespace and virtual ledger; full/partial/reject/unknown scenarios, preserved reservations, no mix with real dashboard. Local UI virtual BTC buy 0.0001 filled and ledger updated. | **Implemented; local and cloud simulation verified.** Two cloud virtual orders remain: normal fill and UNKNOWN reconciled to FILLED under the same intent ID. Data survives new login; scripted execution is not real-market evidence. |
| R13 | Meaningful security, ownership, lifecycle, data and responsive tests | Verified checkpoint: 69 automated tests, TypeScript and production build passed. Auth/API/security, mock Ed25519 transport, data interpretation, account mismatch, stale/expired sessions, duplicate/ambiguous/partial order cases tested. Local desktop/mobile checked. | **69 isolated tests, TypeScript and production build pass.** Cloud auth/logout, storage, responsive UI, table metadata permissions, public reads and normal/unknown simulation are verified. Private broker reads remain blocked on owner credentials. |
| R14 | Continuation docs, environment names only, concise accurate final report | README, HANDOFF, this ledger and official broker contracts document are maintained; source checkpoint in private GitHub. No secrets in docs. | **Implemented.** Production URL and anonymous verification are recorded; preserve later checkpoints and report remaining storage/account blockers without calling the site fully connected. |

## Unified external dependencies

1. **Completed — production storage and server configuration:** Supabase rqxxhpberxhpadgynrnw is connected through verified TLS. All required Vercel Production Secrets are saved. Cloud login, persistence and metadata permissions are verified. Preserve credentials; do not repeat setup.
2. **Revolut X private connection:** user registers their own Ed25519 public key and enters the associated API key/private PEM through the authenticated site after production readiness. Confirm actual EEA/Germany account and scope; a read success does not prove trade permission. IP allowlisting may require available stable egress; no new paid service is authorized.
3. **Optional IBKR connection:** existing authorized account, actual API/data entitlements and a persistent locally authenticated Gateway or explicitly approved alternative. Only then implement/verify its adapter. Do not open a new financial account, accept agreements or buy subscriptions.
4. **Completed for app/storage/public feed/simulation:** cloud login/logout, anonymous boundaries, durable simulation, mobile/desktop and SQL metadata permissions verified. Private-account reads, broker-specific accounting and live order capability remain unverified until authorized account connection.

## Preserved scope decisions

### Independent paper research extension — 23 September 2026

The owner authorized an isolated paper experiment, result logging and transfer to the existing Google Sheet, preserving all application environments and broker connections. `research/README.md` defines the exact benchmark, hypothetical costs, validation and output contract. The independent Node scripts do not import the app or execute broker orders. The workflow publishes public-price research reports on `mirsad-paper-results`; the ChatGPT review task is the Sheets transfer worker. A deployed workflow or populated report must be verified separately from local tests. Historical paper results are not live fills, prospective validation, or evidence of future profit.

Twelve isolated Node tests cover validation, missing/gapped data, duplicate timestamps, deterministic IDs, next-open execution, no future leakage, fees, cash reconciliation and zero-volume handling. The source app, environment files, secrets and broker adapter are unchanged by this extension. Do not treat this extension as enabling unattended financial execution.

- Next.js App Router/TypeScript, server-only official broker adapters, durable SQL state, one owner's password authentication.
- No financial execution during build or testing. The site's future order path serves the user's manual decisions only; the user individually confirms each real order.
- No false balances, positions, returns, FX totals, tradability or buying power. No promised low-risk profit or guaranteed outcome.
- No background investment bot or continuous broker session in short-lived Vercel functions. Polling runs while the app is open; market outages/age remain visible.
- No other project changes or destructive resource operations. No paid plan, brokerage account opening, financial agreement, or funds transfer.
- The prior conversation heartbeat stays **paused**; no ongoing automation was enabled. Work after this session requires a new execution, not an implied promise of continued activity.

